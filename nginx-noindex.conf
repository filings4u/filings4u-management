# Include inside the server block for this management site.
add_header X-Robots-Tag "noindex, nofollow, noarchive, nosnippet, noimageindex" always;
add_header Cache-Control "private, no-store" always;
